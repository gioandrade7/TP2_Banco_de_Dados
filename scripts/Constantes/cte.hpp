#ifndef CTE_HPP
#define CTE_HPP

using namespace std;

const int BLOCO_SIZE = 4096;//Tamanho do bloco em bytes
const int BUCKET_SIZE = 20; //Tamanho do bucket em blocos (quantos blocos o Bucket tem)
const int HASH_SIZE = 15000;//Tamanho da tabela hash (quantos buckets a tabela hash tem)
const int m = 1; //ordem da b+tree

#endif